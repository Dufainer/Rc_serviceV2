@model Rc_serviceV2.Models.Propietario

@{
    ViewData["Title"] = "Create";
}

<style>

    .row {
        width: 460px;
        height: 90%;
        padding: 30px;
        align-items: center;
        align-content: center;
        /*text-align: center;*/
        margin-left: 29%;
        margin-right: 29%;
    }

</style>
<h3 style="text-align: center; padding-top :20px">Agregar nuevo inmueble</h3>
<hr />
<div class="row" style="border: 2px 5px solid #000000;box-shadow: 0 0 10px rgba(0, 0, 0, 0.8); border-radius:30px ">
    <form asp-action="Create" id="createForm">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label  class="control-label">Id del propietario</label>
                <input asp-for="IdPropietario" class="form-control" />
                <span asp-validation-for="IdPropietario" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label  class="control-label">Nombres</label>
                <input asp-for="NamePropietario" class="form-control" />
                <span asp-validation-for="NamePropietario" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label  class="control-label">Apellidos</label>
                <input asp-for="LastnamePropietario" class="form-control" />
                <span asp-validation-for="LastnamePropietario" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Celular" class="control-label"></label>
                <input asp-for="Celular" class="form-control" />
                <span asp-validation-for="Celular" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Email" class="control-label"></label>
                <input asp-for="Email" class="form-control" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label  class="control-label">Direccion</label>
                <input asp-for="UbicacionId" class="form-control" />
                <span asp-validation-for="UbicacionId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            <a asp-action="Index" class="btn btn-dark">Volver</a>
            </div>
        </form>
    </div>

@section Scripts {

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <script>
        $(function () {
            $('#createForm').submit(function (e) {
                e.preventDefault(); // Evitar que el formulario se envíe de forma predeterminada

                // Mostrar la alerta de éxito de SweetAlert
                Swal.fire({
                    icon: 'success',
                    title: '¡Éxito!',
                    text: 'Se ha creado el servicio con éxito.'
                }).then((result) => {
                    if (result.isConfirmed) {
                        // Enviar el formulario para crear el servicio
                        $('#createForm').unbind('submit').submit();
                    }
                });
            });
        });
    </script>

    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

